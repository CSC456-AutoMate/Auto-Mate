---

- name: Update local facts
  ansible.builtin.setup:
    filter: ansible_local

- name: Add provision local fact
  ansible.builtin.include_role:
    name: local_fact
  with_items: "{{ provision__pre_upgrade }}"
  when: ansible_local.provision is not defined

- name: Run apt-get dist-upgrade
  ansible.builtin.apt:
    update_cache: true
    upgrade: dist
    force_apt_get: true
  when: ansible_local.provision.setup.initial_setup is falsy(convert_bool=true)
  changed_when: ansible_local.provision.setup.initial_setup is falsy(convert_bool=true)
  notify:
    - Set provision local fact
    - Reboot system after dist-upgrade
